atb_rk3568j_smc_r1_linux_5_10_110_build:
  stage: build
  tags: [buildroot]
  image: ubuntu:22.04
  timeout: 24h
  before_script:
    - export DEBIAN_FRONTEND=noninteractive
    - apt update
    - apt install -y make cmake git gcc g++ mtools genext2fs cpio rsync bc file
    - apt install -y python2 python3 unzip wget bzip2 dosfstools
  script:
    - ln -s /buildroot/dl dl
    - rm -rf output
    - make atb_rk3568j_smc_r1_linux-5.10.110_defconfig
    - make source
    - make
  cache:
    key: atb_rk3568j_smc_r1_linux_5_10_110
    paths:
    - output/images
    - output/target
  rules:
    - if: $CI_PROJECT_NAME == "atb-buildroot-main"
      when: always
    - if: $CI_PROJECT_NAME != "atb-buildroot-main"
      when: manual

atb_rk3568j_smc_r1_linux_5_10_110_artifact:
  stage: artifact
  tags: [buildroot]
  needs: ["atb_rk3568j_smc_r1_linux_5_10_110_build"]
  script:
    - ls output/images
    - ls output/target
  cache:
    key: atb_rk3568j_smc_r1_linux_5_10_110
    paths:
    - output/images
    - output/target
  artifacts:
    name: atb_rk3568j_smc_r1_linux_5_10_110
    paths:
      - output/target
      - output/images/idblock.bin
      - output/images/uboot.img
      - output/images/Image
      - output/images/atb-rk3568j-smc-r1.dtb
      - output/images/rootfs.cpio.gz
  rules:
    - if: $CI_PROJECT_NAME == "atb-buildroot-main"
      when: always
    - if: $CI_PROJECT_NAME != "atb-buildroot-main"
      when: manual
